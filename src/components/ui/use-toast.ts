
// Re-export toast functions from the hooks location
import { useToast, toast, type ToasterToast, type Toast, type ToastActionElement } from "@/hooks/use-toast";

export { useToast, toast, type ToasterToast, type Toast, type ToastActionElement };
