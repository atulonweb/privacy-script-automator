
// Re-export toast functions from the hooks location
import { 
  useToast, 
  toast, 
  type ToasterToast, 
  type Toast, 
  type ToastActionElement,
  type ToastProps
} from "@/hooks/use-toast";

export { 
  useToast, 
  toast, 
  type ToasterToast, 
  type Toast, 
  type ToastActionElement,
  type ToastProps 
};
